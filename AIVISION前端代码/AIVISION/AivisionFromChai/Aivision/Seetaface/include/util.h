#ifndef __SEETA_UTIL_H
#define __SEETA_UTIL_H

#define min(x, y) (x < y ? x : y)
#define max(x, y) (x > y ? x : y)
#define without(x, min, max) (x < min || x > max)
#define within(x, min, max) (x >= min && x <= max

#endif